#pragma once
#include <raylib.h>

//basic components!!!
typedef Vector3 Position;

typedef int Render;

typedef Camera Camera;

typedef int Health;

typedef Vector3 Target;

struct PlayerTag{};

struct GroundTag{};

struct TitanTag{};
